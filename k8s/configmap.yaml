apiVersion: v1
kind: ConfigMap
metadata:
  name: google-chat-mcp-config
  namespace: qry
  labels:
    app: google-chat-mcp-server
    component: mcp-server
    tier: backend
data:
  LOG_LEVEL: "INFO"
  # Default space can be overridden per tool call
  GOOGLE_CHAT_DEFAULT_SPACE: ""