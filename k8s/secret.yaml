apiVersion: v1
kind: Secret
metadata:
  name: google-chat-mcp-credentials
  namespace: qry
  labels:
    app: google-chat-mcp-server
    component: mcp-server
    tier: backend
type: Opaque
data:
  # This will be populated with the base64-encoded service account JSON
  # kubectl create secret generic google-chat-mcp-credentials \
  #   --from-file=google-chat-mcp-server-key.json=./credentials/google-chat-mcp-server-key.json \
  #   --namespace=qry
  google-chat-mcp-server-key.json: ""